import { SearchUserFilterPipe } from './search-user-filter.pipe';

describe('SearchUserFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchUserFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
