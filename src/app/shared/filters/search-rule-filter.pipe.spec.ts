import { SearchRuleFilterPipe } from './search-rule-filter.pipe';

describe('SearchRuleFilterPipe', () => {
  it('create an instance', () => {
      const pipe = new SearchRuleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
