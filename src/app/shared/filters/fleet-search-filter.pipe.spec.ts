import { FleetSearchFilterPipe } from './fleet-search-filter.pipe';

describe('FleetSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FleetSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
