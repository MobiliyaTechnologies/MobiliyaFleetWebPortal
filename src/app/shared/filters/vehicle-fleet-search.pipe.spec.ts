import { VehicleFleetSearchPipe } from './vehicle-fleet-search.pipe';

describe('VehicleFleetSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleFleetSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
