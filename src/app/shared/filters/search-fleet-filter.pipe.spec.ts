import { SearchFleetFilterPipe } from './search-fleet-filter.pipe';

describe('SearchFleetFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchFleetFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
