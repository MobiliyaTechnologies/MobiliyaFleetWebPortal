import { SearchDeviceFilterPipe } from './search-device-filter.pipe';

describe('SearchDeviceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDeviceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
