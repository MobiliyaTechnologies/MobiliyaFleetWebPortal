import { VehicleSearchFilterPipe } from './vehicle-search-filter.pipe';

describe('VehicleSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new VehicleSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
