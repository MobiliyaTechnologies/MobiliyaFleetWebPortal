import { RuleFilterPipe } from './rule-filter.pipe';

describe('RuleFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RuleFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
